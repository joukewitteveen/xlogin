#! @bash@

# X server sends a SIGUSR1 to its parent process
# when connecting to the server is possible.
# See "man 1 xserver" for details.
trap "exit 0" USR1
(
  trap "" USR1
  exec /usr/bin/X "$@"
) &
wait
exit 1
